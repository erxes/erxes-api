name: Api CI

on:
  push:
    branches:
      - '**'
    paths:
      - '**'
      - '!.github/**'
      - '.github/workflows/api.yaml'
      - '!elkSyncer/**'
      - '!email-verifier/**'
      - '!engages-email-sender/**'
      - '!logger/**'
      - '!**.md'
    tags:
      - '*'
  pull_request:
    branches:
      - master
      - develop
<<<<<<< HEAD
      - golomt
=======
      - crons
      - workers
>>>>>>> c5b4cb49237b44683378387a5e5927fd705b5f45
    paths:
      - '**'
      - '!.github/**'
      - '.github/workflows/api.yaml'
      - '!elkSyncer/**'
      - '!email-verifier/**'
      - '!engages-email-sender/**'
      - '!logger/**'
      - '!**.md'

jobs:
  api:
    runs-on: ubuntu-18.04

    # name: Node ${{ matrix.node-version }}

    services:
      mongodb:
        image: mongo:3.6
        ports:
          - 27017:27017

    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2

    - name: Use Node.js 10.x
      uses: actions/setup-node@v1
      with:
        node-version: 10.x

    # https://github.com/actions/cache/blob/master/examples.md#node---yarn
    - name: Get yarn cache directory path
      id: yarn-cache-dir-path
      run: echo "::set-output name=dir::$(yarn cache dir)"

    - name: Cache Node.js modules
      id: yarn-cache # use this to check for `cache-hit` (`steps.yarn-cache.outputs.cache-hit != 'true'`)
      uses: actions/cache@v1
      with:
        path: ${{ steps.yarn-cache-dir-path.outputs.dir }}
        key: ${{ runner.os }}-yarn-api-${{ hashFiles('yarn.lock') }}
        restore-keys: |
          ${{ runner.os }}-yarn-api-${{ hashFiles('yarn.lock') }}
          ${{ runner.os }}-yarn-api-
          ${{ runner.os }}-yarn-

    - name: Install
      run: |
        yarn install

    - name: Lint
      run: |
        yarn lint

    - name: Tsc
      run: |
        yarn tsc

    - name: Build
      if: github.event_name == 'push' && ( github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/golomt' || github.ref == 'refs/heads/crons' || github.ref == 'refs/heads/workers')
      run: |
        yarn build

    - name: Build docker image [push]
      if: github.event_name == 'push' && ( github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/golomt' || github.ref == 'refs/heads/crons' || github.ref == 'refs/heads/workers')
      run: |
        echo ${{ secrets.DOCKERHUB_TOKEN }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin
        docker build -t erxes/erxes-api:${GITHUB_REF#refs/heads/} -f Dockerfile .
        docker push erxes/erxes-api:${GITHUB_REF#refs/heads/}

    - name: Build docker image [tag]
      if: github.event_name == 'push' && contains(github.ref, 'refs/tags/')
      run: |
        docker build -t erxes/erxes-api:${GITHUB_REF#refs/tags/} -f Dockerfile .
        docker push erxes/erxes-api:${GITHUB_REF#refs/tags/}
